services:
  payment-db:
    image: postgres:latest
    container_name: payment-service-db
    restart: always
    env_file:
      - .env
    volumes:
      - ../../docker-data/payment-db:/var/lib/postgresql/data
    ports:
      - '${POSTGRES_PORT}:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-payment_db}']
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - multivendor-network

networks:
  multivendor-network:
    external: true
